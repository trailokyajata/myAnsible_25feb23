---
- hosts: 172.31.9.161
  become: yes
  tasks:
  - name: execute linux directly
    command: hostname -f
    register: myresult


  - name: i want to display first task result
    debuga:
      msg: "{{myresult}}"